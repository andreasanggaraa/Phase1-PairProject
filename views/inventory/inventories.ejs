<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Users</title>
    <%- include('../partials/bootstrap-head.ejs'); %>
</head>

<body>
    <%- include('../partials/navbar.ejs') %>

    <table class="table table-striped" style="width:85%;margin-top: 50px;" align="center">
        <tr>
          <th>#</th>
          <th>Name</th> 
          <th>Position</th>
          <th>Action</th>
        </tr>
        <% for( let i = 0; i < inventories.length; i++ ) { %>
            <tr>
                <td><%= inventories[i].id %> </td>
                <td><%= inventories[i].stockName %> </td>
                <td><%= inventories[i].quantity %> </td>
              </tr>
        <% } %>
      </table>

<%- include('../partials/bootstrap-script.ejs'); %>
</body>

</html>