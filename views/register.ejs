<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register New Account</title>
    <style>
      #errorMessage {color: red;}
    </style>
    <%- include('../partials/bootstrap-head.ejs'); %>
</head>
<body>
    <%- include('../partials/navbar.ejs', {page: "cast"}) %>

    <form style="margin-left: 10px; margin-right: 10px" action="/cast/add" method="POST">

        <div class="form-group">
          <label for="name">First Name:</label><br>
          <input class="form-control" type="text" id="first_name" name="first_name">
          <small id="errorMessage"><%= errorMessage.first_name ? `${errorMessage.first_name}` : "" %> </small>
        </div>

        <div class="form-group">
          <label for="last_name">Last Name:</label><br>
          <input class="form-control" type="text" id="last_name" name="last_name">
          <small id="errorMessage"><%= errorMessage.last_name ? `${errorMessage.last_name}` : "" %> </small>
        </div>

        <div class="form-group">
          <label for="phone_number">Phone Number:</label><br>
          <input class="form-control" type="text" id="phone_number" name="phone_number">
          <small id="errorMessage"><%= errorMessage.phone_number ? `${errorMessage.phone_number}` : "" %> </small>
        </div>

        <div class="form-group">
          <label for="birth_year">Birth Year:</label><br>
          <input class="form-control" type="number" id="birth_year" name="birth_year">
          <small id="errorMessage"><%= errorMessage.birth_year ? `${errorMessage.birth_year}` : "" %> </small>
        </div>

        <label for="gender">Gender:</label><br>

        <div class="form-check form-check-inline">
          <input class="form-check-input" type="radio" name="gender" id="gender" value="female" checked>
          <label class="form-check-label" for="gender" name="gender">Female</label>
        </div>
        <div class="form-check form-check-inline">
          <input class="form-check-input" type="radio" name="gender" id="gender" value="male">
          <label class="form-check-label" for="gender" name="gender">Male</label>
        </div>
        <br><br>
        <input type="submit" value="Add" class="btn btn-primary">
        <a href="/cast"><button type="button" class="btn btn-danger">Cancel</button></a>
      </form>
      <%- include('../partials/bootstrap-script.ejs'); %>
</body>
</html>