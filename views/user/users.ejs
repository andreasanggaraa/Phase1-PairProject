<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Users</title>
    <%- include('../partials/bootstrap-head.ejs'); %>
</head>

<body>
    <%- include('../partials/navbar.ejs') %>

    <table class="table table-striped" style="width:85%;margin-top: 50px;" align="center">
        <tr>
          <th>id</th>
          <th>Nama</th> 
          <th>Cup Purchased</th>
          <th>Action</th>
        </tr>
        <% for( let i = 0; i < users.length; i++ ) { %>
            <tr>
                <td><%= users[i].id %> </td>
                <td><%= users[i].fullName %> </td>
                <td><%= users[i].purchaseToReward %> </td>
                <td>
                  <a href="/edit/<%= users[i].id %>"> Edit  </a> |
                  <a href="/delete/<%= users[i].id %>"> Delete  </a> |
                  <a href='/redeem/<%= users[i].id %>'> <%= users[i].purchaseToReward >= 10 ? "Redeem" : "" %>  </a>

                </td>
              </tr>
        <% } %>
      </table>

<%- include('../partials/bootstrap-script.ejs'); %>
</body>

</html>